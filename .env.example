# Server-only secrets (never VITE_)
XAI_API_KEY=
XAI_VOICE_AGENT_ID=
XAI_TTS_VOICE=ara

# demo_historical | keeper_only
VITE_CONTENT_CORPUS=demo_historical

# Live public API (when Keeper backend ships GET /api/v1/public/lexicon)
VITE_API_BASE_URL=

# Engineering: test living-forms pipeline without backend (filters sacred mocks).
# Defaults ON in local `npm run dev` only — not in production builds.
VITE_USE_MOCK_PUBLIC_API=true

VITE_KEEPER_PORTAL_URL=https://kuttiomp-admin-viti.vercel.app
